# T-002 — Architecture du coffre ZERO-KNOWLEDGE
- **Owner** : `/securite` (+ `/module-infra`) · **Priorité** : P0 · **État** : **EN RECETTE** (🟢 livré) · **Vague** : S0–S2 · **Déclencheur** : réalisé (T-001 🟢 + `atelier` intégré)
- **Amont** : **T-001** 🟢. · **Aval** : prérequis de **T-004**.
- **Livré (preuve)** : rapport `/securite` 🟢 ; **PR #8** ouverte (archi ZK : dérivation Argon2id → KEK/DEK par item, AEAD, **récupération 4 facteurs sans déchiffrement serveur** ; **10/18 P0 couverts par conception**, reco *1 secret maître*). Intégration vague 2 dispatchée (Release). Clôture *FAIT* gated par VERIFIED prod + 0 P0.

## Contexte
Spécifier l'architecture cryptographique garantissant que le serveur ne peut JAMAIS lire les données, avec récupération « zéro perte » sans déchiffrement serveur.

## Périmètre / fichiers (writer unique)
`orchestration/agents/securite/ARCHI-ZERO-KNOWLEDGE.md` (writer : `/securite` ; `/module-infra` contribue via prompt/route, ne co-écrit pas le même fichier).

## Critères d'acceptation (DoD)
- [ ] Chiffrement **E2E** défini ; **dérivation de clé** à partir d'un secret utilisateur (phrase de récupération + biométrie).
- [ ] **Récupération sans déchiffrement serveur** : codes de secours hors-ligne + récupération sociale (contacts de confiance) + clé matérielle.
- [ ] **Sync multi-appareils chiffrée** + stratégie de résolution de conflits.
- [ ] Modèle de stockage serveur (chiffré, aveugle) + rotation/révocation de clés.
- [ ] Rapport 🟢/🔴 + SEC-### si écarts.

## Clôture (anti-récidive)
Commit précis + verdict QA.
